include ':app', ':settings', ':nasa', ':pixelsort', ':shared'
rootProject.name='KmpPlayground'

//project(':app').projectDir = new File('android/app')

// move all android specific modules into android folder
file('android').eachDir { dir ->
  project(":${dir.name}").projectDir = dir
}
